# EAS Build ignore file
# Files listed here will be excluded from EAS builds
# Use ! prefix to INCLUDE files that are in .gitignore

# Include google-services.json even though it's in .gitignore
!google-services.json
!GoogleService-Info.plist

# Exclude other files (same as .gitignore but we explicitly include Firebase files above)
node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
npm-debug.*
yarn-debug.*
yarn-error.*
.DS_Store
*.pem
.env*.local
.env
.env.production
.env.development
*.tsbuildinfo
firebase_service_account.json

