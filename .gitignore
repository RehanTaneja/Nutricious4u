# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# IDE and editors
.idea/
.vscode/

# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Production builds
/build
dist/
dist

# Environment files (comprehensive coverage)
.env*
.env.local
.env.development.local
.env.test.local
.env.production.local
*token.json*
*credentials.json*

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb

# System files
.DS_Store
*.pem

# Python
__pycache__/
*pyc*
venv/
.venv/

# Development tools
chainlit.md
.chainlit
.ipynb_checkpoints/
.ac

# Deployment
.vercel

# Data and databases
agenthub/agents/youtube/db

# Archive files and large assets
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb
*.dylib

# Build caches
.cache/

# Mobile development
android-sdk/ 
backend/services/firebase_service_account.json

# Security - Firebase and API keys
**/firebase_service_account.json
# **/google-services.json - REMOVED: Required for EAS builds (safe to commit, not a secret)
# **/GoogleService-Info.plist - REMOVED: Required for EAS builds (safe to commit, not a secret)
**/*.pem
**/*.key
**/*_key.json
**/*credentials.json
**/*token.json
**/*secret.json

# Environment variables
.env*
*.env
.env.local
.env.development
.env.test
.env.production

# Private keys and certificates
**/*private*
**/*cert*
**/*.p12
**/*.pfx

# Firebase credentials
backend/services/firebase_service_account.json
**/firebase_service_account.json
# **/google-services.json - REMOVED: Required for EAS builds (safe to commit, not a secret)
# **/GoogleService-Info.plist - REMOVED: Required for EAS builds (safe to commit, not a secret)

# Environment files
.env
.env.local
.env.production
.env.development

# API keys and secrets
**/secrets.json
**/credentials.json
**/*.pem
**/*.key

mobileapp/nutricious4u-63158-firebase-adminsdk-fbsvc-75599d1f92.json
